# Профиль: убрана инструкция по Telegram, добавлено фото профиля

1. На странице «Профиль» из блока Telegram Chat ID убран текст-инструкция
   («Найдите бота…», «Напишите боту @userinfobot…» и т.д.) — оставлено
   только поле для ввода Chat ID.
2. Добавлена возможность загрузить/сменить/удалить фото профиля (клик по
   аватару или кнопка «Изменить фото»). Фото сжимается в браузере до
   512×512 и сохраняется вместе с остальными полями профиля по кнопке
   «Сохранить изменения».
3. В базу данных добавлено поле `avatarUrl` у пользователя (Prisma
   `schema.prisma`), API `/api/profile` теперь читает и сохраняет его.

---

# Роль «PR Manager» + новые права в вкладке «Задачи»

## Что нового

1. **Новая роль `PR_MANAGER`** — выбирается при создании/редактировании
   аккаунта (страница «Пользователи», доступна ADMIN). По правам — как
   MANAGER, но дополнительно умеет ставить задачи.

2. **PR Manager может ставить задачи**: Амбассадорам, другим PR-менеджерам,
   Администраторам и Директору (раньше исполнителем мог быть только
   ADMIN/MANAGER). Список «Кому поставить задачу» на странице `/tasks`
   теперь включает все эти роли.

3. **Амбассадор и Директор получили доступ к вкладке «Задачи»**
   (`/tasks`) — раньше им был доступен только раздел «Приглашённые
   клиенты», «Отчёты», «Моя команда» и профиль.

4. **Для Амбассадора вкладка «Задачи» урезана**: только просмотр своих
   задач + две кнопки — «Выполнено» и «Не выполнено». Кнопок «Передать»
   и «Удалить» амбассадор не видит (и не может выполнить эти действия
   через API — проверено и на сервере, не только в интерфейсе).

5. **«Не выполнено» требует обязательной причины**: при нажатии
   открывается модальное окно с текстовым полем — без заполнения
   отправить нельзя (ни на клиенте, ни на сервере: `POST` без `note`
   вернёт ошибку 400 «Укажите причину невыполнения»). Причина сохраняется
   в `Task.notDoneReason` и отображается прямо на карточке задачи
   (плюс попадает в историю изменений).

6. Задача в статусе «Не выполнено» — отдельный статус (`NOT_DONE`, кроме
   `OPEN`/`DONE`), с красным бейджем. В фильтре «В работе» такие задачи
   тоже показываются (это ещё не завершённые успешно задачи).

## Как это работает по ролям

- **ADMIN / MANAGER** — без изменений, полный доступ к задачам (как раньше).
- **PR_MANAGER** — то же самое, что MANAGER, + видит вкладку «Задачи» и
  может назначать её на Ambassador/PR_MANAGER/ADMIN/DIRECTOR.
- **DIRECTOR** — получил доступ к «Задачам» (может выполнять полный набор
  действий над своими задачами: выполнено/не выполнено/передать/удалить,
  как обычный участник задачи — создавать новые задачи по-прежнему нельзя).
- **AMBASSADOR** — получил доступ к «Задачам» в урезанном виде: видит
  только свои задачи, отмечает «выполнено» / «не выполнено» (с обязательной
  причиной), не может создавать, передавать или удалять задачи.

## Изменённые / новые файлы

```
prisma/schema.prisma                       — Role.PR_MANAGER; TaskStatus.NOT_DONE; Task.notDoneReason
src/lib/roles.ts                           — Role type, canCreateTasks(), доступ /tasks и /api/tasks для AMBASSADOR/DIRECTOR
src/lib/i18n/translations.ts               — новые ключи (ru/pl/uk): role_pr_manager, task_status_not_done,
                                              mark_not_done_btn, not_done_reason_*, task_log_not_done и т.д.
src/components/UsersAdmin.tsx              — PR_MANAGER в списке ролей (создание/редактирование пользователя)
src/app/tasks/page.tsx                     — список "кому назначить" включает Ambassador/PR_Manager/Admin/Director
src/app/api/tasks/route.ts                 — создание задачи проверяется через canCreateTasks (не canEdit)
src/app/api/tasks/[id]/route.ts            — action "not_done" (обязательная причина), запрет transfer/edit для AMBASSADOR
src/app/api/users/list/route.ts            — расширенный список ролей (на будущее, для выбора исполнителя)
src/app/team/page.tsx                      — PR_MANAGER виден в "Моей команде"
src/app/api/team/route.ts                  — то же самое для API
src/components/TasksBoard.tsx              — статус NOT_DONE, кнопки "Выполнено"/"Не выполнено", модалка причины,
                                              скрытие "Передать"/"Удалить" для AMBASSADOR
```

## Как применить

Скопируйте файлы поверх соответствующих путей в вашем репозитории
(структура папок сохранена), закоммитьте и запушьте — Railway пересоберёт
проект автоматически и применит новую схему БД через `prisma db push`
(это уже прописано в `npm start`, отдельно ничего мигрировать не нужно).

```bash
npm install
npx prisma generate
npm run build   # локальная проверка перед пушем
```

## Важно

Я собирал этот патч в песочнице без доступа к npm registry и
`binaries.prisma.sh`, поэтому не смог прогнать `npm install`,
`prisma generate` и `next build` с реальными типами. Код прошёл
ручную проверку (внимательно сверил все места, где раньше был
`status: "OPEN" | "DONE"`, чтобы новый статус `NOT_DONE` не сломал
типы) — но обязательно прогоните `npm run build` локально или
дождитесь сборки на Railway перед тем как считать это финальным.

Если сборка на Railway упадёт — пришлите мне лог ошибки, поправлю.

После деплоя не забудьте создать хотя бы одного пользователя с ролью
**PR Manager** (страница «Пользователи» → «+ Новый пользователь» → роль
«PR Manager»), иначе ставить задачи амбассадорам будет некому.
